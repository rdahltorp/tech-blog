<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- CSS only -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
    <title>Tech Blog</title>
</head>

<body>
    <header class="p-3 mb-2 bg-info text-dark">
        <h1>Tech Blog</h1>
        <nav>
            <button class="btn btn-outline-dark"><a href="/" class="text-reset text-decoration-none">Home</a></button>
            <button class="btn btn-outline-dark"><a href="/api/users/dashboard" class="text-reset text-decoration-none">Dashboard</a></button>
            {{#if logged_in}}
            <button id="logout" class="btn btn-outline-dark">Logout</button>
            {{else}}
            <button class="btn btn-outline-dark"><a href="/api/users/login" class="text-reset text-decoration-none">Login</a></button>
            {{/if}}
        </nav>
    </header>

    <main class="d-grid gap-3 p-3">
        {{{body}}}
    </main>
    <footer>
    </footer>
    
    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    {{/if}}
</body>
</html>