{{#each posts as |post| }}
<div>
    <div>
        <h2>{{post.title}}</h2>
        <p>Posted by: {{post.user.username}} on {{post.date_created}}</p>
    </div>
    <div>
        <p>{{post.description}}</p>
    </div>
</div>
{{/each}}